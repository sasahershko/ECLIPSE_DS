package com.utad.inso.tema2.entregas.patronFactory.ejercicio35;

public class MobileClientTest {
    public static void main(String[] args) {
        //Mobile phone = MobileFactoryManager.getInstance().createMobile(TypeMobile.IPHONE);

        //System.out.println(phone.getBrand() + "with language " + phone.getLanguage());
    }
}
